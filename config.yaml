# Evrensel yarışma ayarları — tüm stratejiler için tek kaynak (CLAUDE.md kural 6).
# Değerler taslaktır, onay sonrası netleştirilecek.

universe:
  symbols: []          # örn. ["BTCUSDT", "ETHUSDT"]
  timeframe: "4h"

account:
  starting_balance_usd: 10000.0
  commission_rate: 0.0004   # taker, tek yön

funding:
  enabled: true

schedule:
  run_interval: "4h"
