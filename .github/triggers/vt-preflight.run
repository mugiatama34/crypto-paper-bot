# measure-vol-target: ÖN-KONTROL (docs/backtest.md > 6o > 15, TADİLAT-1). Hiçbir getiri,
# σ̂, ağırlık ya da Sharpe üretmez: 13 sembolün 4H serisinin kapsamı (ilk/son bar, eksik gün).
# Tekrarlanabilir; ölçümün tek seferlik tetikleyicisi AYRI dosyadır (vt-measure*.run).
# Bu dosya bir daha DEĞİŞTİRİLMEZ.
