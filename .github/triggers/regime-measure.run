# measure-regime: ÖLÇÜM — TEK SEFER (docs/backtest.md > 6l > 7).
# Kullanıcı onayı: 2026-09-24, TADİLAT-2 ve sonuç öncesi beklenti (34122eb) kayda girdikten sonra.
# Ön-kontrol #35993682181 temiz: 6/6 portföy + 52/52 tek-sembollü defter, orijinal xsec
# results.json mevcut, BTC 2020-10-30'dan, eksik gün 0, ilk tanımlı gün 2021-11-28.
# Bu dosyanın EKLENDİĞİ commit koşunun kendisidir; dosya bir daha DEĞİŞTİRİLMEZ.
# Yeni bir regime-measure*.run yalnızca bu koşu HİÇBİR SAYI ÜRETMEDEN düşerse eklenir (§7.6).
