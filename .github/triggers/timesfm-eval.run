# measure-timesfm: DEĞERLENDİRME koşusu (docs/backtest.md > 6k > 8). P0 → P1 → dönem A → (A geçerse) B ve C.
# Parite sonucu bu dosyadan ÖNCE kayda geçti (§6k > 13, commit 6a3e167).
# Bu dosyanın EKLENDİĞİ commit koşunun kendisidir; dosya bir daha DEĞİŞTİRİLMEZ.
