# measure-regime: ÖN-KONTROL (docs/backtest.md > 6l > TADİLAT-2 > 3). Hiçbir R, pozisyon
# sayısı ya da rejim ataması üretmez: artifact dosyalarının varlığı + BTC serisinin kapsamı.
# Tekrarlanabilir; ölçümün tek seferlik tetikleyicisi AYRI dosyadır (regime-measure*.run).
# Bu dosya bir daha DEĞİŞTİRİLMEZ.
