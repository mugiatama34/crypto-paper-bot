# measure-timesfm: PARİTE koşusu, ikinci tetikleme (docs/backtest.md > 6k > 5). Yalnızca P0 + P1; isabet YOK.
# Gerekçe: ilk tetikleme (timesfm-parity.run, koşu #35853769262) workflow'un aşama
# çözümünde, betik HİÇ ÇALIŞMADAN durdu (head_commit.added boş). Hiçbir sayı üretilmedi
# ve görülmedi; bu yüzden bu bir "sonucu görüp tekrar koşma" DEĞİLDİR.
# Bu dosyanın EKLENDİĞİ commit koşunun kendisidir; dosya bir daha DEĞİŞTİRİLMEZ.
